# Clock-alignment port diffs (B <- A)

Generated for merge planning. A = Twowheelers_18_5 FusionApp, B = Github FusionApp.

See agent report for focused symbol/section extracts; unified diffs may be regenerated with:

```
git diff --no-index -- "B/file" "A/file"
```
